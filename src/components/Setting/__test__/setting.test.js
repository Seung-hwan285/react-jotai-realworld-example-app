describe('settingForm', () => {
  test('should have inital state with empty and username,bio,email,image', () => {});

  test('', () => {
    document.cookie =
      'myCookie=test; expires=Fri, 31 Dec 9999 23:59:59 UTC; path=/';

    // const cookies = document.cookie.split(';').reduce((cookies, cookie) => {
    //   const [name, value] = cookie.trim().split('=');
    //   return { ...cookies, [name]: value };
    // }, {});

    // expect(cookies['myCookie']).toEqual('test');
  });
});
